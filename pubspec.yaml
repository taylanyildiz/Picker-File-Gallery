name: bottom_sheet_picker
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  
  get: ^4.3.8
  camera: ^0.9.4+3
  snapping_sheet: ^3.1.0
  photo_manager: ^1.3.9+1
  font_awesome_flutter: ^9.2.0
  video_player: ^2.2.6
  image_crop: ^0.4.0
  video_trimmer: ^0.6.0
  path_provider: ^2.0.6
  file_picker: ^4.2.2+1
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
     - assets/images/
